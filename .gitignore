# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Never ship a build of the theme from inside the theme
*.zip

# Local video sources (encoded files are uploaded to the media library)
*.mov
21-source.mp4

# Local design preview (hardcoded absolute paths, not part of the theme)
preview.html
